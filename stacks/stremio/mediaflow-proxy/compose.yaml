services:
  mediaflow-proxy:
    container_name: mediaflow-proxy
    hostname: mediaflow-proxy
    image: mhdzumair/mediaflow-proxy:v2.4.8
    restart: unless-stopped
    ports:
      - 8888:8888
    networks:
      - proxy-network
    env_file:
      - ${DOCKER_DATA}/mediaflow-proxy/.env
    logging:
      options:
        max-size: 1g
    deploy:
      resources:
        limits:
          cpus: "0.5"
    labels:
      backrest.backup: "true"
      backrest.priority: "low"
      backrest.action: "none"
    
networks:
  proxy-network:
    name: ${EXTERNAL_NETWORK_NAME:-proxy-network}
    external: true
