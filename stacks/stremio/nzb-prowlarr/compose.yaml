services:  
  prowlarr:
    image: ghcr.io/hotio/prowlarr:release-2.3.0.5236
    container_name: nzb-prowlarr
    hostname: nzb-prowlarr
    restart: unless-stopped
    ports:
      - 9797:9696
    networks:
      - proxy-network
    environment:
      PUID: ${PUID}
      PGID: ${PGID}
      TZ: ${TZ}
    volumes:
      - ${DOCKER_DATA}/nzb-prowlarr/config:/config
      - ${DOCKER_DATA}/nzb-prowlarr/custom-definitions:/config/Definitions/Custom
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    
networks:
  proxy-network:
    name: ${EXTERNAL_NETWORK_NAME:-proxy-network}
    external: true