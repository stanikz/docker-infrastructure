services:
  code-server:
    image: lscr.io/linuxserver/code-server:4.111.0-ls323
    container_name: code-server
    hostname: code-server
    restart: unless-stopped
    ports:
      - 8444:8443
    networks: 
      - proxy-network
    environment:
      PUID: ${PUID}
      PGID: ${PGID}
      TZ: ${TZ}
      #All below environment variables are optional
      PASSWORD: 
      SUDO_PASSWORD: 
      HASHED_PASSWORD: ${VSCODE_PASSWORD} 
      SUDO_PASSWORD_HASH: ${VSCODE_PASSWORD} 
      PROXY_DOMAIN: ${VSCODE_DOMAIN} 
      DEFAULT_WORKSPACE: /config/workspace
      PWA_APPNAME: code-server
    volumes:
      - ${DOCKER_DATA}/vscode-server/config:/config

networks:
  proxy-network:
    name: ${EXTERNAL_NETWORK_NAME:-proxy-network}
    external: true