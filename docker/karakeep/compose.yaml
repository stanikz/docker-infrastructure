services:
  web:
    image: ghcr.io/karakeep-app/karakeep:${KARAKEEP_VERSION:-0.30.0}
    restart: unless-stopped
    container_name: karakeep-web
    volumes:
      - ${DOCKER_DATA}/karakeep/data:/data
    ports:
      - 4321:3000
    environment:
      MEILI_ADDR: http://meilisearch:7700
      BROWSER_WEB_URL: http://chrome:9222
      OPENAI_API_KEY: ${GEMINI_API_KEY}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL}
      INFERENCE_TEXT_MODEL: ${AI_MODEL} 
      INFERENCE_IMAGE_MODEL: ${AI_MODEL}
      EMBEDDING_TEXT_MODEL: ${AI_MODEL}
      NEXTAUTH_URL: ${NEXTAUTH_URL}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
      OAUTH_WELLKNOWN_URL: ${OAUTH_WELLKNOWN_URL}
      OAUTH_CLIENT_SECRET: ${OAUTH_CLIENT_SECRET}
      OAUTH_CLIENT_ID: ${OAUTH_CLIENT_ID}
      OAUTH_PROVIDER_NAME: ${OAUTH_PROVIDER_NAME}
      OAUTH_ALLOW_DANGEROUS_EMAIL_ACCOUNT_LINKING: true
      DISABLE_PASSWORD_AUTH: false
      DISABLE_SIGNUPS: true
      DATA_DIR: /data # DON'T CHANGE THIS
    networks:
      - myNetwork
      
  chrome:
    image: gcr.io/zenika-hub/alpine-chrome:124
    container_name: karakeep-chrome
    restart: unless-stopped
    command:
      - --no-sandbox
      - --disable-gpu
      - --disable-dev-shm-usage
      - --remote-debugging-address=0.0.0.0
      - --remote-debugging-port=9222
      - --hide-scrollbars
    networks:
      - myNetwork
      
  meilisearch:
    image: getmeili/meilisearch:v1.32.1 # v1.22.0
    restart: unless-stopped
    container_name: karakeep-milisearch
    environment:
      MEILI_NO_ANALYTICS: "true"
    volumes:
      - ${DOCKER_DATA}/karakeep/meilisearch:/meili_data
    networks:
      - myNetwork
    #ports:
    #  - 7700:7700  #--> THIS PART IS ONLY NEEDED WHEN UPGRADING AND IMPORTING THE DB DUMP <---
    #command: >
    #  meilisearch --import-dump /meili_data/dumps/20260114-134531436.dump
    
    
networks:
  myNetwork:
    external: true